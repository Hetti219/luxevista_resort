#========================================
# Gradle files
#========================================
.gradle/
build/

# Ignore Gradle build cache (useful for Android Studio 3.5+)
.gradle-build-cache/

# Ignore generated Gradle wrapper files (if you choose not to commit them, though committing is usually recommended)
# gradle/wrapper/gradle-wrapper.jar
# gradle/wrapper/gradle-wrapper.properties

#========================================
# Local configuration
#========================================
# Contains SDK path, NDK path, and potentially sensitive credentials like keystore passwords
local.properties

#========================================
# Android Studio / IntelliJ IDEA files
#========================================
# Module files (generated by Gradle sync)
*.iml
# IDE project files (user-specific settings, caches, etc.)
.idea/caches/
.idea/libraries/
.idea/modules.xml
.idea/workspace.xml
.idea/navEditor.xml
.idea/assetWizardSettings.xml
# NOTE: Some teams commit specific .idea files like shared run configurations (.idea/runConfigurations/)
# or dictionaries (.idea/dictionaries/). By default, it's safer to ignore the whole .idea folder,
# but you can explicitly un-ignore (!<pattern>) specific files/folders if your team agrees.
# Example:
# .idea/
# !.idea/runConfigurations/ # Uncomment to track shared run configurations

#========================================
# Build output & generated files
#========================================
# APKs, App Bundles, intermediate files
*.apk
*.aab
*.ap_
*.a_
*.dex
*.class

# Files from the jobb tool
*.obb

# Older generated folders (less common now, but good for legacy)
bin/
gen/
out/

# Android Studio Navigation editor temp files
.navigation/

# Android Studio Profiler snapshots & captures
captures/
*.hprof

# Lint reports (unless you want to track them)
lint-results*.xml
lint-results*.html

# External native build folder (for NDK projects)
.externalNativeBuild/

#========================================
# Keystores & Signing Keys
#========================================
# IMPORTANT: Never commit your release keystore files! Keep them private and secure.
# It's best practice to store them outside the project directory entirely.
*.jks
*.keystore

#========================================
# Google Services
#========================================
# The google-services.json file might contain project identifiers but usually not highly sensitive keys.
# However, it's often downloaded via the IDE or CI/CD rather than committed directly.
# Uncomment the line below if you manage this file manually and prefer not to commit it.
# google-services.json

#========================================
# Log Files & Temporary Files
#========================================
*.log

#========================================
# OS-specific files
#========================================

# Windows
Thumbs.db
desktop.ini


#========================================
# Crashlytics files (if used locally)
#========================================
# com_crashlytics_export_strings.xml
# crashlytics.properties
# crashlytics-build.properties
# fabric.properties

#========================================
# Optional: Add any other tool-specific or custom generated files below
#========================================